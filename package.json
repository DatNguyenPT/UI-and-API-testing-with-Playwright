{
  "name": "sourcedemo-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test UI/",
    "test:api": "npx playwright test API/",
    "test:smoke": "npx playwright test --grep @smoke",
    "test:regression": "npx playwright test --grep @regression",
    "test:critical": "npx playwright test --grep @critical",
    "test:ui:smoke": "npx playwright test UI/ --grep @smoke",
    "test:ui:regression": "npx playwright test UI/ --grep @regression",
    "test:api:smoke": "npx playwright test API/ --grep @smoke",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report",
    "allure:generate": "npx allure generate allure-results --clean -o allure-report",
    "allure:open": "npx allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/debug": "^4.1.12",
    "@types/node": "^25.1.0",
    "allure-playwright": "^3.4.5"
  },
  "dependencies": {
    "debug": "^4.4.3",
    "dotenv": "^17.2.3"
  }
}
